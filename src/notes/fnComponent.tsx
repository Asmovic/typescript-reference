import React, { FC, useState } from 'react';

interface IProps{};
interface IState{};

let Component:FC<IProps> = () => {
    return (
        <React.Fragment>
            <h3>Component</h3>
        </React.Fragment>
    )
};

export default Component;